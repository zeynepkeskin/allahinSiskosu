"use client";

import Link from "next/link";
import { useState } from "react";
import { Button, Card, EmptyState } from "@/components/ui";
import { days, exercisePrescription, type Exercise, type ExercisePlan } from "@/lib/exercises";

const blankExercise = (): Exercise => ({ name: "", sets: 3, reps: 10, weightLb: null, weightDisplay: "", restSeconds: 45 });
type Session = { id: string; status: "completed" | "ended_early" | "in_progress"; startedAt: string };
export function ExercisePlanner({ initialPlans, sessions }: { initialPlans: ExercisePlan[]; sessions: Session[] }) {
  const [plans, setPlans] = useState(initialPlans); const [editingDay, setEditingDay] = useState<number>(); const [draft, setDraft] = useState<{ isRestDay: boolean; exercises: Exercise[] }>({ isRestDay: false, exercises: [blankExercise()] }); const [message, setMessage] = useState<string>(); const [saving, setSaving] = useState(false);
  function open(day: number) { const plan = plans.find((item) => item.dayOfWeek === day); setEditingDay(day); setDraft(plan ? { isRestDay: plan.isRestDay, exercises: plan.exercises.map((exercise) => ({ ...exercise })) } : { isRestDay: false, exercises: [blankExercise()] }); setMessage(undefined); }
  function updateExercise(index: number, field: keyof Exercise, value: string) { setDraft((current) => ({ ...current, exercises: current.exercises.map((exercise, i) => i === index ? { ...exercise, [field]: field === "name" || field === "weightDisplay" ? value : value === "" ? (field === "weightLb" ? null : 0) : Number(value) } : exercise) })); }
  async function save() { if (editingDay === undefined) return; setMessage(undefined); if (!draft.isRestDay && draft.exercises.some((exercise) => !exercise.name.trim() || exercise.sets < 1 || exercise.reps < 1)) { setMessage("Add a name, sets, and reps for every exercise."); return; } setSaving(true); const response = await fetch(`/api/exercises/${editingDay}`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(draft) }); const body = await response.json().catch(() => ({})); setSaving(false); if (!response.ok) { setMessage(body.error ?? "Could not save plan."); return; } setPlans((current) => [...current.filter((plan) => plan.dayOfWeek !== editingDay), body].sort((a, b) => a.dayOfWeek - b.dayOfWeek)); setEditingDay(undefined); }
  return <div className="mt-8 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><section className="space-y-3">{days.map((day, index) => { const plan = plans.find((item) => item.dayOfWeek === index); return <Card className="p-0" key={day}><div className="flex items-start justify-between gap-4 px-5 py-4"><div><p className="font-semibold">{day}</p>{plan?.isRestDay ? <p className="mt-1 text-sm font-medium text-slate-500">REST</p> : plan ? <div className="mt-2 space-y-1">{plan.exercises.map((exercise, i) => <p className="text-sm text-slate-600" key={`${exercise.name}-${i}`}>{exercisePrescription(exercise)}</p>)}</div> : <p className="mt-1 text-sm text-slate-500">No workout planned</p>}</div><div className="flex shrink-0 gap-3">{plan && !plan.isRestDay ? <Link className="rounded-xl bg-emerald-600 px-3 py-2 text-sm font-semibold text-white hover:bg-emerald-700" href={`/exercises/${index}`}>Start</Link> : null}<button className="text-sm font-semibold text-emerald-700" onClick={() => open(index)} type="button">{plan ? "Edit" : "Plan"}</button></div></div></Card>; })}<Card><h2 className="font-semibold">Recent workouts</h2>{sessions.length ? <div className="mt-3 divide-y divide-slate-100">{sessions.map((session) => <div className="flex justify-between gap-3 py-3 text-sm" key={session.id}><span>{new Intl.DateTimeFormat("en", { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" }).format(new Date(session.startedAt))}</span><span className={session.status === "completed" ? "font-semibold text-emerald-700" : "font-semibold text-slate-500"}>{session.status === "completed" ? "Completed" : session.status === "ended_early" ? "Ended early" : "In progress"}</span></div>)}</div> : <p className="mt-2 text-sm text-slate-500">Completed workouts will appear here.</p>}</Card></section><aside>{editingDay === undefined ? <EmptyState title="Plan your training week" description="Choose a day to add a workout or mark it as rest." /> : <Card><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-emerald-600">{days[editingDay].toUpperCase()}</p><h2 className="mt-1 text-xl font-bold">Workout plan</h2></div><button className="text-sm font-semibold text-slate-500" onClick={() => setEditingDay(undefined)} type="button">Close</button></div><label className="mt-5 flex items-center gap-3 rounded-xl bg-slate-50 p-3 text-sm font-medium"><input checked={draft.isRestDay} onChange={(event) => setDraft((current) => ({ ...current, isRestDay: event.target.checked, exercises: event.target.checked ? [] : current.exercises.length ? current.exercises : [blankExercise()] }))} type="checkbox" /> REST day</label>{!draft.isRestDay ? <div className="mt-5 space-y-4">{draft.exercises.map((exercise, index) => <fieldset className="rounded-xl border border-slate-200 p-3" key={index}><div className="flex justify-between gap-3"><legend className="font-semibold">Exercise {index + 1}</legend>{draft.exercises.length > 1 ? <button className="text-sm font-semibold text-red-600" onClick={() => setDraft((current) => ({ ...current, exercises: current.exercises.filter((_, i) => i !== index) }))} type="button">Remove</button> : null}</div><input aria-label={`Exercise ${index + 1} name`} className="mt-3 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm" placeholder="Bicep curls" value={exercise.name} onChange={(event) => updateExercise(index, "name", event.target.value)} /><div className="mt-3 grid grid-cols-2 gap-3"><Field label="Sets" value={exercise.sets} onChange={(value) => updateExercise(index, "sets", value)} min="1" /><Field label="Reps" value={exercise.reps} onChange={(value) => updateExercise(index, "reps", value)} min="1" /><Field label="Total weight (lb)" value={exercise.weightLb ?? ""} onChange={(value) => updateExercise(index, "weightLb", value)} min="0" /><Field label="Rest (seconds)" value={exercise.restSeconds} onChange={(value) => updateExercise(index, "restSeconds", value)} min="0" /></div><input aria-label="Weight display" className="mt-3 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm" placeholder="Optional: 25 lb per hand" value={exercise.weightDisplay} onChange={(event) => updateExercise(index, "weightDisplay", event.target.value)} /></fieldset>)}<button className="text-sm font-semibold text-emerald-700" onClick={() => setDraft((current) => ({ ...current, exercises: [...current.exercises, blankExercise()] }))} type="button">+ Add exercise</button></div> : null}{message ? <p aria-live="polite" className="mt-4 text-sm text-red-600">{message}</p> : null}<div className="mt-6 flex gap-3"><Button disabled={saving} onClick={save} type="button">{saving ? "Saving…" : "Save plan"}</Button><button className="text-sm font-semibold text-slate-600" onClick={() => setEditingDay(undefined)} type="button">Cancel</button></div></Card>}</aside></div>;
}
function Field({ label, value, onChange, min }: { label: string; value: number | string; onChange: (value: string) => void; min: string }) { return <label className="text-xs font-medium text-slate-600">{label}<input className="mt-1 w-full rounded-lg border border-slate-300 px-2 py-2 text-sm" min={min} onChange={(event) => onChange(event.target.value)} type="number" value={value} /></label>; }
